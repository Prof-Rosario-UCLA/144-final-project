Planning

message objects contain a chatID field, which references the chat it belongs in
    -helps w pagination

routes for messages:
    -[DONE] send a msg (w/ or w/o media files) & emit to socket.io room
        -/upload for multimedia
    -[DONE] get msgs for some chat (up to a limit/before some time) -- pagination is a frontend deal
    -add read flag maybe? depends on chat schema

routes for chats:
    -[DONE] create chat or open current chat (upon req) 
    -[DONE] list all chats
    -may need to overhaul and add lastMessageSent and lastTimeRead per chat for notif handling 
        -how much should be handled on FE


Chats should have some schema field or route indicator on latest message sent in it (for frontend ordering)
[DONE] for chats and msgs routes, they need to work on server.js run

routes for users:
    -[DONE] register
    -set username (is this the only profile modification?)
    -pfp maybe
    -[DONE] login
    -[DONE] search users by UN - regex
    -[DONE] get own user info

socket stuff:
    -[DONE] when a msg object is emitted to the room, send it to all participants
    -[DONE] implement immediate update on rcv on frontend
    -do notif feature and update order of displayed chats

SSO:
    -[DONE] Google Oauth
    -[DONE] look into jwt tokens, bcrypt
    -[DONE] make /middleware/auth stuff

frontend:
    -[DONE] login page
        -[DONE] viewability
        -[DONE] actual sso impl
    -set username page - easy af
        -[DONE] viewability
        -[DONE] actual sso impl
    -[DONE] chatting area once you login
    -pagination handling for both chats and messages per chat
    -displaying media types in chat and chat's latest message preview

no need for caching

object store:
    -video, audio, images
    -GCP Cloud Storage (GCS)
    -get obj store URL upon getting the page of the chat it belongs in

camera & mic:
    -figure this out later?
    -frontend deal where it sends the blob using /upload path (msg)



